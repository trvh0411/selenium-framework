# Selenium Framework

Automation testing framework using Selenium + TestNG + Maven.

## Requirements
- Java 17
- Maven
- Chrome browser

## Run test (local)

mvn clean test -Dbrowser=chrome -Denv=dev -DsuiteXmlFile=testng-smoke.xml
